# KrakenWhip — RAG Pipeline Configuration
# Copy to .env and fill in your values

# Embedding model (HuggingFace model ID)
# KRAKENWHIP_EMBEDDING_MODEL=BAAI/bge-small-en-v1.5

# Qdrant collection name
# KRAKENWHIP_COLLECTION_NAME=documents

# RAG API port (default: 8080)
# KRAKENWHIP_PORT=8080
