# Agents — Dave IT Guy / OpenClaw

This file is required by OpenClaw for workspace bootstrap. **Do not delete.**

## Primary agent

You run inside the **Dave IT Guy** OpenClaw stack (Ollama, Qdrant, optional MasterClaw). Use workspace scripts only under `/home/node/.openclaw/workspace` — see `TOOLS.md` for `simple_search.py`, `simple_qdrant.py`, and `simple_subagent.py`.

Behave helpfully, prefer tools when they improve accuracy, and respect memory/Qdrant settings from the main configuration.
